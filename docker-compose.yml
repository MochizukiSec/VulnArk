version: '3.8'

services:
  # MongoDB数据库服务
  mongodb:
    image: mongo:latest
    container_name: vuln-mongodb
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-password}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE:-vulnmanagement}
    volumes:
      - mongodb_data:/data/db
    ports:
      - "27017:27017"
    networks:
      - vuln-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/vulnmanagement --quiet
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # 后端服务
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vuln-backend
    restart: always
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      MONGO_URI: mongodb://${MONGO_USERNAME:-admin}:${MONGO_PASSWORD:-password}@mongodb:27017/${MONGO_DATABASE:-vulnmanagement}?authSource=admin
      JWT_SECRET: ${JWT_SECRET:-your_jwt_secret_key}
      PORT: 8000
    ports:
      - "8000:8000"
    networks:
      - vuln-network

  # 前端服务
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: vuln-frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
    networks:
      - vuln-network

# 数据卷
volumes:
  mongodb_data:
    driver: local

# 网络
networks:
  vuln-network:
    driver: bridge 